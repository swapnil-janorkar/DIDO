apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dido-ingress
spec:
  rules:
  - host: dido.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dido-frontend-service
            port:
              number: 3000

